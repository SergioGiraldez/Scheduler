class Category:
	def __init__(self, available_hours, tasks, first_date, total_dificulty):
		self.available_hours = available_hours
		self.tasks = tasks
		self.first_date = first_date
		self.total_dificulty = total_dificulty
